/**
 * @fileoverview Placeholder test to verify the Vitest test framework is wired up correctly.
 */

import { describe, it, expect } from 'vitest';

describe('test framework', () => {
  it('should run a basic assertion', () => {
    expect(1 + 1).toBe(2);
  });
});
